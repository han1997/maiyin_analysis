{"file": ".trellis/spec/backend/tauri-contract.md", "reason": "Verify export_result command signature now carries on_progress Channel but returns same OperationResult; verify OperationResult { message, path } structure unchanged; verify CSV BOM + formula-injection prefixing still applied; verify progress emits validation-before-emit (no progress on error paths)."}
{"file": ".trellis/spec/backend/quality-guidelines.md", "reason": "Verify the 'Tauri 2 Channel-based progress reporting' pattern is followed (throttle 50ms, domain callback Tauri-agnostic, progress is companion state not gating); verify no #[cfg(test)] leaked in production paths."}
{"file": ".trellis/tasks/07-28-export-perf-progress-chinese/research/reference-export-format.md", "reason": "Verify all three export formats match the reference: summary_csv 18 cols (split household + frequency_window_count), raw_csv 23 cols (split household + age/gender + region), risk_xlsx 26 cols merged-row + ALERT_KIND_LABELS Chinese + per-level colors + freeze + autofilter + sheet name '风险合并明细'."}
{"file": ".trellis/spec/frontend/type-safety.md", "reason": "Verify AppApi.exportResult onProgress is optional (backward compatible); Progress type reused from prior task; browser adapter ignores onProgress; no any casts."}
{"file": ".trellis/spec/frontend/quality-guidelines.md", "reason": "Verify progress UI is inline (not modal), reuses existing progress-line; reduced-motion still renders determinate width; no third-party UI lib introduced; export menu entry points unchanged."}
