{"file": ".trellis/spec/backend/tauri-contract.md", "reason": "Command signatures (import_paths/import_folders/reanalyze/merge_sessions) we add on_progress Channel param to; WorkspaceSnapshot contract that must stay unchanged; camelCase serde convention for ProgressPayload; spawn_blocking boundary rules; error matrix for validation_error/empty_import."}
{"file": ".trellis/spec/backend/quality-guidelines.md", "reason": "Backend conventions (Test-gated timing helpers shape, Centralized sheet selection) — new progress callback pattern should match the project's existing helper-externalization style; verify no #[cfg(test)] leaks in production paths."}
{"file": ".trellis/spec/frontend/state-management.md", "reason": "Existing loading state pattern (busy enum, runSnapshotAction funnel at App.tsx:235) — the new progress state plugs in alongside; must not break the single busy-action invariant."}
{"file": ".trellis/spec/frontend/quality-guidelines.md", "reason": "Frontend conventions, progressive disclosure, reduced-motion handling (styles.css:718 auto-disables animations) — the new progress bar must respect these."}
{"file": ".trellis/spec/frontend/type-safety.md", "reason": "AppApi contract (src/api/contract.ts) we extend with optional onProgress; Progress type addition; browser fixture adapter must stay compatible (ignore onProgress)."}
